# requirements/phase0.txt
#
# Phase: 0
# Specs: SPEC-LIB-001, SPEC-LIB-002, SPEC-LIB-003
# Owner: Platform
# Consumers: ingestion, datastore, config, scheduler, tests
#
# Bootstrap dependencies: data layer, ingestion, scheduler, and test tooling.
# All versions pinned with == per SPEC-LIB-001 (no >= or ~=). Upgrades follow
# the SPEC-LIB-002 protocol: one library at a time, on a branch, full test
# suite + walk-forward backtest comparison before merge.

pandas==2.2.3
numpy==1.26.4
pyarrow==23.0.1
duckdb==1.2.0
sqlalchemy==2.0.36
# [DEPENDENCY AUDIT, 2026-06-24] Force-upgraded requests, aiohttp, setuptools
# past fyers-apiv3==3.1.13's hard pins to clear pip-audit CVEs:
#   requests: CVE-2024-35195/CVE-2024-47081/CVE-2026-25645 → fixed in 2.32.0+
#   aiohttp:  33 CVEs in 3.9.3 → fixed in 3.14.1
#   setuptools: 3 CVEs in 68.0.0 → fixed in 70.0.0+ / 78.1.1+
# fyers-apiv3 3.1.13 hard-requires these older versions, so pip warns about
# the conflict but does not block installation. Verified 2026-06-24: importing
# fyers_apiv3 succeeds cleanly after the upgrade; all unit tests pass.
# pip-audit now reports: "No known vulnerabilities found".
# Re-evaluate if FyersDev ships a new release that relaxes its version pins.
requests==2.34.2
aiohttp==3.14.1
setuptools==82.0.1
beautifulsoup4==4.12.3
APScheduler==3.11.0
python-dotenv==1.2.2
tqdm==4.68.3
fyers-apiv3==3.1.13
pytest==9.0.3
pytest-cov==6.0.0
# Pipeline & Monitoring Remediation Phase 2/4 (2026-07-10): accurate
# per-process RSS for ingestion/scheduler/resource_guard.py and the
# real-time Ops resource monitor, replacing monitor_scheduler_resources.py's
# deliberately hand-rolled /proc/meminfo parsing. resource_guard.py falls
# back to /proc parsing if psutil isn't installed, so this is additive,
# not a hard runtime break.
psutil==6.1.1
